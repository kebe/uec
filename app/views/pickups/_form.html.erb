<%= form_with(model: pickup, local: true) do |form| %>
  <% if pickup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pickup.errors.count, "error") %> prohibited this pickup from being saved:</h2>

      <ul>
      <% pickup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :pickup_status %>
  </div>

  <div class="field">
    <%= form.label :day %>
    <%= form.text_field :day, id: :pickup_day %>
  </div>

  <div class="field">
    <%= form.label :pickup_date %>
    <%= form.date_select :pickup_date, id: :pickup_pickup_date %>
  </div>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id, id: :pickup_client_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
